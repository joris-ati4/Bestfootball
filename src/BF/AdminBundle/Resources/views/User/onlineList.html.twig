<p id="who-is-online">
    <strong>Utilisateurs en ligne ({{ users|length }}) :</strong>.

    <ul>
    	{% for user in users %}
    		<li><a href="{{ path('bf_site_admin_user_view', {'id': user.id} ) }}"> {{ user.username }}</a></li>
    	{% endfor %}
    </ul>


</p>