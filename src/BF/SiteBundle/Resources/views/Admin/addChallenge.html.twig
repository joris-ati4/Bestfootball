{% extends "BFSiteBundle::base.html.twig" %}


{% block meta %}
  <meta name="description" content="Bla bla bla concernant l'upload.">
  <meta name="keywords" content="soccer,football ">
{% endblock %}


{% block title %}
  Admin Section- add a new challenge - {{ parent() }} 
{% endblock %}

{% block content %}

  {# src/BF/SiteBundle/Resources/views/Video/form.html.twig #}
  <section class="header">
    <div class="row" style="margin-top:20px;">
      <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 text-center">
        <div class="row">
          <h3>Add a new Challenge</h3>
            <div class="well">
            {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}

              {# Les erreurs générales du formulaire. #}
              {{ form_errors(form) }}

              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.titleEN, "Title English:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.titleEN) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.titleEN, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>

              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.titleFR, "Title French:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.titleFR) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.titleFR, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>

              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.descriptionEN, "Description EN:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.descriptionEN) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.descriptionEN, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>

              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.descriptionFR, "Description FR:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.descriptionFR) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.descriptionFR, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>

              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.one, "Minimum value first level:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.one) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.one, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>

              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.two, "Minimum value second level:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.two) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.two, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>

              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.three, "Minimum value third level:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.three) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.three, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>
              
              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.four, "Minimum value fourth level:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.four) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.four, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>

              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.five, "Minimum value fifth level:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.five) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.five, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>

              <div class="form-group">
                {# Génération du label. #}
                {{ form_label(form.six, "Minimum value highest level:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

                {# Affichage des erreurs pour ce champ précis. #}
                {{ form_errors(form.six) }}

                <div class="col-sm-4">
                  {# Génération de l'input. #}
                  {{ form_widget(form.six, {'attr': {'class': 'form-control'}}) }}
                </div>
              </div>
            {{ form_rest(form) }}
            {{ form_end(form) }}
          </div>




        </div>
      </div>
    </div>
  </section>




  
{% endblock %}
