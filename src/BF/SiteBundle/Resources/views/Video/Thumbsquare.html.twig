	<div class="col-xs-12 col-sm-6 col-md-3 col-lg-3 text-center" style="padding:5px 5px;">
		<div class="row">
			<a href="{{ path('bf_site_video', { 'id': video.id }) }}">
	        	<img src="{{ video.thumbUrl }}" alt="{{ video.thumbAlt }}"  style="width:90%;"/>
	        	<p style="text-align:left;">{{ video.title }}</br><a href="{{ path('bf_site_profile', { 'username': video.user.username }) }}">{{ video.user.username }}</a></p>
        	</a>
    	</div>
    </div>
