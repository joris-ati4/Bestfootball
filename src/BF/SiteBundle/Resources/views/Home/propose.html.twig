{% extends "BFSiteBundle::base.html.twig" %}


{% block meta %}
  <meta name="description" content="Bla bla bla concernant l'upload.">
  <meta name="keywords" content="soccer,football ">
{% endblock %}


{% block title %}
  Start a new Duel - {{ parent() }} 
{% endblock %}

{% block content %}

  
<section class="profile">
  <div class="row">
    <div class="col-xs-10 col-sm-10 col-md-10 col-lg-10 col-xs-offset-1 col-sm-offset-1 col-md-offset-1 col-lg-offset-1">
      <div class="challengedescription text-center" style="margin:150px 0;">
        <div class="challengetitle">
          <h3 style="color:white;">Start a new duel!</h3>
        </div>
        <div class="challengecontent">
          {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
          {# Les erreurs générales du formulaire. #}
          {{ form_errors(form) }}

          {# Idem pour un autre champ. #}
          <div class="form-group">
            {{ form_label(form.challenge, "Message:", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
            {{ form_errors(form.challenge) }}
            <div class="col-sm-4">
              {{ form_widget(form.challenge, {'attr': {'class': 'form-control'}}) }}
            </div>
          </div>

          <input type="submit" value="Create the challenge" class="btn btn-warning" />
        {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
        {{ form_rest(form) }}
        {# Fermeture de la balise <form> du formulaire HTML #}
        {{ form_end(form) }}
        </div>
      </div>
    </div>
  </div>
</section>
{% endblock %}